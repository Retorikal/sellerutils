create_dictionary
  - Pull https://en.digimoncard.com/cardlist/
  - Cek booster list dari web
  - For each booster name: 
    - If dict file exists, skip
    - Otherwise create

Dictionary JSON Format
{
  CCODE-001:{
    name: "name"
    variants: [ // To be edited manually
      "_P1 Variant Name",
      "_P2 Variant Name"
    ]
  },
  etc..
}

verify_stock
  - get stock csv file
  - if non-standard stock is found without entry:
    - raise warning

Stock CSV Json Format
code, vanillastock, p1stock, p2stock, etc..
